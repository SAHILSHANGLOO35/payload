import { Invoice } from "@/types/invoice"
import React from "react"

type InvoiceProps = {
  invoice: Invoice
}

export const ItemsTable = ({ invoice }: InvoiceProps) => {
  return <div>ItemsTable</div>
}
